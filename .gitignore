# Exclude all contents of the build directory
/build

# Exclude binaries
/super-glue

# Exclude compile-commands, as these can be generated from the Makefile
compile_commands.json

# Exclude clangd caches
/.cache/

# Exclude user-specific GDB files
.gdb*

# Exclude vim swp files
.*.swp
